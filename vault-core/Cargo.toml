[package]
name = "vault-core"
version = "0.1.0"
edition = "2021"
authors = ["Vault Team"]
description = "Core cryptographic library for encrypted vault containers"
license = "MIT OR Apache-2.0"

[lib]
name = "vault_core"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
# Cryptographic dependencies
libsodium-sys = "0.2"
aes-gcm = "0.10"
argon2 = "0.5"
hkdf = "0.12"
x25519-dalek = "2.0"

# Serialization and utilities
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.0", features = ["v4", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# Error handling and logging
thiserror = "1.0"
log = "0.4"

# FFI and C compatibility
libc = "0.2"

# Random number generation
rand = "0.8"
getrandom = "0.2"

# Hash functions
sha2 = "0.10"

# Base64 encoding
base64 = "0.21"

[dev-dependencies]
hex = "0.4"
tempfile = "3.0"

[features]
default = ["std"]
std = []

[profile.release]
lto = true
codegen-units = 1
panic = "abort"